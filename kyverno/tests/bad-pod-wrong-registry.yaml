apiVersion: v1
kind: Pod
metadata:
  name: bad-pod-wrong-registry
spec:
  containers:
    - name: eks-monitor
      image: nginx:1.21
      securityContext:
        runAsNonRoot: true
      resources:
        limits:
          cpu: "500m"
          memory: "256Mi"
